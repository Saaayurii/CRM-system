-- Заказчик отказался от восстановления по SMS (дорого).
-- Удаляем таблицу одноразовых кодов (хранила только транзиентные OTP, ценных данных нет).
-- Журнал восстановлений account_recovery_log сохраняется как есть (колонка method остаётся
-- для исторических записей; новые восстановления всегда method='email').
DROP INDEX IF EXISTS idx_prc_phone;
DROP TABLE IF EXISTS phone_reset_codes;
