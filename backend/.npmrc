# Isolated node_modules (pnpm default): each service gets its own @prisma/client,
# so the 23 distinct Prisma schemas don't clobber a single shared generated client.
node-linker=isolated
strict-peer-dependencies=false
